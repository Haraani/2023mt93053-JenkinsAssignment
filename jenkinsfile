pipeline {
     agent any
     stages {
        
        stage('Build') {
            steps {
               script{
                     python setup.py build // Build the project
               }
            }
        }    
    }
}
